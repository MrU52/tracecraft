version: '3.8'

services:
  tracecraft:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tracecraft-app
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
